# Render Blueprint for EduBridge (Combined Frontend + Backend)
# Documentation: https://render.com/docs/blueprint-spec

services:
  - type: web
    name: edubridge
    env: node
    plan: free
    region: oregon  # Choose the region closest to your users
    buildCommand: |
      # Install dependencies
      npm install
      cd apps/web
      npm install
      npm install -D vite@latest
      
      # Build frontend
      npm run build
      
      # Move frontend build to server public directory
      cd ../..
      mkdir -p server/public
      cp -r apps/web/dist/* server/public/
      
      # Install server dependencies
      cd server
      npm install
    
    startCommand: node server.js
    
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: GEMINI_API_KEY
        sync: false  # Set this in Render dashboard
      - key: GEMINI_MODEL
        value: gemini-1.5-flash
      - key: VITE_API_URL
        value: /api  # API requests will be proxied through the same domain
    
    healthCheckPath: /api/ping
    autoDeploy: true
        value: https://edubridge-backend.onrender.com  # Update with your backend URL
    autoDeploy: true
